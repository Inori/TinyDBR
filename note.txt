
ModRM 
==================
mov rbx, [rcx + 0x40]

-------------------------------

pushaq
pushfq

sub rsp, 20
and rsp, 0xFFFFFFFFFFFFFFF0
mov rcx, this
lea rdx, [rcx+0x40]
mov r8, 8
mov rax, OnMemoryRead

call rax

popfq
popaq
mov rbx, [rcx + 0x40]

-------------------------------


mov [rcx + 0x40], rbx

pushaq
pushfq

mov rcx, this
lea rdx, [rcx + 0x40]
mov r8, 8
mov r9, rbx
call OnMemoryWrite

popfq
popaq
mov [rcx + 0x40], rbx


movs
==================


load/store
==================