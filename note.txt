
ModRM 
==================
mov rbx, [rcx + 0x40]

-------------------------------

pushaq
pushfq

mov rcx, this
lea rdx, [rcx+0x40]
mov r8, 8
call OnMemoryRead

popfq
popaq
mov rbx, [rcx + 0x40]

-------------------------------


mov [rcx + 0x40], rbx

pushaq
pushfq

mov rcx, this
lea rdx, [rcx + 0x40]
mov r8, 8
mov r9, rbx
call OnMemoryWrite

popfq
popaq
mov [rcx + 0x40], rbx


movs
==================


load/store
==================